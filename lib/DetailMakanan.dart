import 'package:flutter/material.dart';

class Detailmakanan extends StatelessWidget {
  const Detailmakanan({super.key, required mealId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}